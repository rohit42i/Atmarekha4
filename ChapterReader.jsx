import { useState, useEffect, useRef, useCallback } from 'react';
import axios from 'axios';
import { track } from './analytics.js';

const ReaderLoader = ({ page }) => <div className="ar-loader" aria-label={`Loading page ${page}`}><div className="ar-aura" /><div className="ar-ring" /><div className="ar-symbol"><svg viewBox="0 0 220 220" aria-hidden="true"><path className="ar-mark" d="M110 38 C155 60 170 90 145 110 C120 130 75 120 70 155" /><path className="ar-mark ar-two" d="M65 75 C95 45 135 55 150 85 C165 115 140 145 105 155" /><path className="ar-mark ar-three" d="M110 40 C90 75 95 100 110 110 C125 120 130 145 110 180" /></svg></div><div className="ar-fragments">{Array.from({ length: 24 }, (_, i) => <i key={i} className={`ar-fragment ar-f${i + 1}`} />)}</div><div className="ar-thread ar-thread-one" /><div className="ar-thread ar-thread-two" /><div className="ar-burst">{Array.from({ length: 12 }, (_, i) => <i key={i} style={{ '--r': `${i * 30}deg` }} />)}</div><div className="ar-node ar-node-one" /><div className="ar-node ar-node-two" /><div className="ar-center" /><span className="ar-loading-text">Preparing page {page}</span><style>{`.ar-loader{position:absolute;inset:0;min-height:240px;display:grid;place-items:center;background:rgba(250,250,250,.92);overflow:hidden;z-index:10;color:#18181b}.dark .ar-loader{background:rgba(9,9,11,.94);color:#fff}.ar-loader>*{position:absolute}.ar-aura{width:120px;height:120px;border-radius:50%;background:radial-gradient(circle,rgba(0,0,0,.045),transparent 65%);animation:arAura 4s ease-in-out infinite}.dark .ar-aura{background:radial-gradient(circle,rgba(255,255,255,.045),transparent 65%)}.ar-ring{width:174px;height:174px;border:1px solid currentColor;border-radius:50%;opacity:.16;animation:arRingMove 8s linear infinite,arRingPulse 4s ease-in-out infinite}.ar-symbol{inset:20px;animation:arSymbol 12s ease-in-out infinite}.ar-symbol svg{width:100%;height:100%;overflow:visible}.ar-mark{fill:none;stroke:currentColor;stroke-width:1.3;stroke-linecap:round;stroke-linejoin:round;stroke-dasharray:80 180;animation:arFlow 3.2s linear infinite,arMarkPulse 4s ease-in-out infinite}.ar-two{stroke-dasharray:45 220;animation-delay:-1.7s}.ar-three{stroke-dasharray:25 260;animation-delay:-2.6s}@keyframes arSymbol{0%{transform:rotate(-12deg) scale(.92)}50%{transform:rotate(12deg) scale(1.04)}100%{transform:rotate(-12deg) scale(.92)}}@keyframes arFlow{from{stroke-dashoffset:0}to{stroke-dashoffset:-260}}@keyframes arMarkPulse{0%,100%{opacity:.12}40%{opacity:.7}60%{opacity:.35}}@keyframes arAura{0%,100%{transform:scale(.8);opacity:.25}50%{transform:scale(1.15);opacity:.8}}@keyframes arRingMove{0%{transform:translate(-50%,-50%) rotate(0deg) scale(.98)}50%{transform:translate(-50%,-50%) rotate(180deg) scale(1.015)}100%{transform:translate(-50%,-50%) rotate(360deg) scale(.98)}}@keyframes arRingPulse{0%,100%{opacity:.25}50%{opacity:.55}}`}</style></div>;

export default function ChapterReader({ chapterId, onBack }) {
    const [chapter, setChapter] = useState(null);
    const [loading, setLoading] = useState(true);
    const apiBaseUrl = import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000';
    useEffect(() => { let cancelled=false; const fetchChapter=async()=>{try{const res=await axios.get(`${apiBaseUrl}/api/chapters/detail/${chapterId}`);if(!cancelled){setChapter(res.data);track('chapter_open',{chapter_id:chapterId,chapter_number:res.data?.chapterNumber,title:res.data?.title})}}catch(err){console.error('Failed to fetch chapter:',err);track('chapter_load_failed',{chapter_id:chapterId})}finally{if(!cancelled)setLoading(false)}};if(chapterId)fetchChapter();return()=>{cancelled=true}},[chapterId,apiBaseUrl]);
    useEffect(() => { if (!chapter) return undefined; const title = `Chapter ${chapter.chapterNumber}${chapter.title ? ` — ${chapter.title}` : ''} | Atma Rekha`; const description = chapter.description || `Read Chapter ${chapter.chapterNumber}${chapter.title ? `: ${chapter.title}` : ''} of Atma Rekha.`; document.title = title; let meta=document.querySelector('meta[name="description"]'); if(!meta){meta=document.createElement('meta');meta.name='description';document.head.appendChild(meta)} const previousDescription=meta.getAttribute('content'); meta.setAttribute('content',description.slice(0,160)); let canonical=document.querySelector('link[rel="canonical"]'); if(!canonical){canonical=document.createElement('link');canonical.rel='canonical';document.head.appendChild(canonical)} const previousCanonical=canonical.getAttribute('href'); canonical.href=`${window.location.origin}${window.location.pathname}#chapter/${chapterId}`; return()=>{document.title='Atma Rekha — Indian Manga'; if(previousDescription===null)meta.removeAttribute('content');else meta.setAttribute('content',previousDescription); if(previousCanonical===null)canonical.removeAttribute('href');else canonical.setAttribute('href',previousCanonical)} },[chapter,chapterId]);
    if(loading)return <div className="flex min-h-screen items-center justify-center bg-zinc-50 dark:bg-zinc-950"><div className="relative h-10 w-10 animate-spin rounded-full border-2 border-zinc-300 border-t-pink-500 dark:border-zinc-800 dark:border-t-pink-500"/></div>;
    if(!chapter)return <div className="flex min-h-screen flex-col items-center justify-center bg-zinc-50 dark:bg-zinc-950"><div className="mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-zinc-100 text-zinc-400 dark:bg-zinc-900 dark:text-zinc-600"><i className="fa-solid fa-book-open text-2xl"/></div><h2 className="text-xl font-bold text-zinc-900 dark:text-[var(--text-color)]">Chapter not found</h2><p className="mt-2 text-zinc-500">This chapter may have been removed or doesn't exist.</p><button onClick={onBack} className="mt-6 rounded-lg bg-blue-600 px-6 py-2 font-semibold text-[var(--text-color)] transition hover:bg-blue-700">Go Back</button></div>;
    return <div className="min-h-screen bg-zinc-50 dark:bg-zinc-950"><div className="fixed top-0 z-50 w-full border-b border-zinc-200 bg-[var(--card-bg)]/95 backdrop-blur-md dark:border-zinc-800 dark:bg-zinc-950/95"><div className="mx-auto flex max-w-5xl items-center gap-4 px-6 py-4"><button onClick={onBack} className="flex h-10 w-10 items-center justify-center rounded-full text-zinc-500 transition hover:bg-zinc-100 hover:text-zinc-900 dark:hover:bg-zinc-800 dark:hover:text-[var(--text-color)]" title="Go back" aria-label="Go back"><i className="fa-solid fa-arrow-left"/></button><div className="flex-1 min-w-0"><h1 className="truncate text-lg font-bold text-zinc-900 dark:text-[var(--text-color)]">Chapter {chapter.chapterNumber}</h1><p className="truncate text-sm text-zinc-500 dark:text-zinc-400">{chapter.title}</p></div></div></div><div className="pt-20 pb-12">{chapter.pdfUrl&&<div className="mx-auto max-w-5xl px-6"><div className="overflow-hidden rounded-2xl border border-zinc-200 bg-[var(--card-bg)] shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><iframe src={`${apiBaseUrl}${chapter.pdfUrl}`} className="h-[85vh] w-full" title="Chapter PDF"/></div></div>}{chapter.pages&&chapter.pages.length>0&&<SwipeableReader pages={chapter.pages} apiBaseUrl={apiBaseUrl} chapterId={chapterId}/>} {!chapter.pdfUrl&&(!chapter.pages||chapter.pages.length===0)&&<div className="mx-auto max-w-3xl px-6"><div className="flex flex-col items-center justify-center rounded-2xl border-2 border-dashed border-zinc-200 bg-[var(--card-bg)] py-20 dark:border-zinc-800 dark:bg-zinc-900"><div className="mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-zinc-100 text-zinc-300 dark:bg-zinc-800 dark:text-zinc-600"><i className="fa-solid fa-file-circle-xmark text-2xl"/></div><h3 className="font-semibold text-zinc-900 dark:text-[var(--text-color)]">No content available</h3><p className="mt-1 text-zinc-500">This chapter hasn't been uploaded yet.</p></div></div>}</div></div>;
}

function SwipeableReader({ pages, apiBaseUrl, chapterId }) {
    const [currentIndex,setCurrentIndex]=useState(0),[loadedPages,setLoadedPages]=useState(()=>new Set()),[pageLoading,setPageLoading]=useState(true),[touchStart,setTouchStart]=useState(null),[touchEnd,setTouchEnd]=useState(null);
    const preloaded=useRef(new Map()),loading=useRef(new Set());
    const minSwipeDistance=50;
    const pageUrl=useCallback(i=>`${apiBaseUrl}${pages[i]}`,[apiBaseUrl,pages]);
    const preloadPage=useCallback((i,priority=false)=>{if(i<0||i>=pages.length||preloaded.current.has(i)||loading.current.has(i))return;loading.current.add(i);const img=new Image();img.decoding='async';img.loading=priority?'eager':'lazy';img.fetchPriority=priority?'high':'low';img.onload=async()=>{try{if(img.decode)await img.decode()}catch(_){}preloaded.current.set(i,img);loading.current.delete(i);setLoadedPages(prev=>{const next=new Set(prev);next.add(i);return next})};img.onerror=()=>loading.current.delete(i);img.src=pageUrl(i)},[pageUrl,pages.length]);
    const warmWindow=useCallback(i=>[i-1,i,i+1].forEach((target,offset)=>preloadPage(target,offset===1)),[preloadPage]);
    useEffect(()=>{setPageLoading(!loadedPages.has(currentIndex));warmWindow(currentIndex)},[currentIndex,warmWindow,loadedPages]);
    useEffect(()=>{const keep=new Set([currentIndex-1,currentIndex,currentIndex+1,currentIndex+2]);for(const [key,img] of preloaded.current){if(!keep.has(key)){img.src='';preloaded.current.delete(key)}}},[currentIndex]);
    useEffect(()=>{if(currentIndex===pages.length-1)track('chapter_complete',{chapter_id:chapterId,total_pages:pages.length})},[currentIndex,pages.length,chapterId]);
    const nextPage=useCallback(()=>{if(currentIndex<pages.length-1){const next=currentIndex+1;preloadPage(next,true);warmWindow(next);setCurrentIndex(next);window.scrollTo({top:0,behavior:'instant'})}},[currentIndex,pages.length,preloadPage,warmWindow]);
    const prevPage=useCallback(()=>{if(currentIndex>0){const prev=currentIndex-1;preloadPage(prev,true);warmWindow(prev);setCurrentIndex(prev);window.scrollTo({top:0,behavior:'instant'})}},[currentIndex,preloadPage,warmWindow]);
    useEffect(()=>{const h=e=>{if(e.key==='ArrowRight'||e.key===' '){e.preventDefault();nextPage()}else if(e.key==='ArrowLeft'){e.preventDefault();prevPage()}};window.addEventListener('keydown',h);return()=>window.removeEventListener('keydown',h)},[nextPage,prevPage]);
    const onTouchStart=e=>{setTouchEnd(null);setTouchStart(e.targetTouches[0].clientX)},onTouchMove=e=>setTouchEnd(e.targetTouches[0].clientX),onTouchEnd=()=>{if(touchStart==null||touchEnd==null)return;const d=touchStart-touchEnd;if(d>minSwipeDistance)nextPage();else if(d<-minSwipeDistance)prevPage()};
    const currentImage=preloaded.current.get(currentIndex);
    return <div className="mx-auto max-w-3xl px-4 min-h-[80vh] flex flex-col justify-center select-none" onTouchStart={onTouchStart} onTouchMove={onTouchMove} onTouchEnd={onTouchEnd}><div className="relative mb-6"><div className="relative min-h-[240px] overflow-hidden rounded-lg bg-zinc-100 dark:bg-zinc-900 shadow-sm border border-zinc-200 dark:border-zinc-800">{pageLoading&&<ReaderLoader page={currentIndex+1}/>} {currentImage?<img key={currentIndex} src={currentImage.src} alt={`Page ${currentIndex+1}`} className="block w-full h-auto object-contain max-h-[85vh] mx-auto" decoding="async" draggable="false" onLoad={()=>setPageLoading(false)}/>:<img key={currentIndex} src={pageUrl(currentIndex)} alt={`Page ${currentIndex+1}`} className="block w-full h-auto object-contain max-h-[85vh] mx-auto" fetchPriority="high" decoding="async" onLoad={()=>{preloadPage(currentIndex,true);setPageLoading(false)}}/>}<div className="absolute inset-y-0 left-0 w-1/4 cursor-pointer opacity-0 hover:opacity-100 transition-opacity bg-gradient-to-r from-black/10 to-transparent flex items-center justify-start pl-4" onClick={prevPage} title="Previous Page" aria-label="Previous page">{currentIndex>0&&<i className="fas fa-chevron-left text-3xl text-[var(--text-color)]/70 drop-shadow-md"/>}</div><div className="absolute inset-y-0 right-0 w-1/4 cursor-pointer opacity-0 hover:opacity-100 transition-opacity bg-gradient-to-l from-black/10 to-transparent flex items-center justify-end pr-4" onClick={nextPage} title="Next Page" aria-label="Next page">{currentIndex<pages.length-1&&<i className="fas fa-chevron-right text-3xl text-[var(--text-color)]/70 drop-shadow-md"/>}</div></div></div><div className="fixed bottom-0 left-0 right-0 bg-[var(--card-bg)] dark:bg-zinc-950 border-t border-zinc-200 dark:border-zinc-800 p-4 shadow-lg z-40"><div className="max-w-3xl mx-auto flex items-center justify-between gap-4"><button onClick={prevPage} disabled={currentIndex===0} aria-label="Previous page" className="flex-1 py-3 rounded-xl bg-zinc-100 dark:bg-zinc-800 text-zinc-900 dark:text-[var(--text-color)] font-medium disabled:opacity-30 disabled:cursor-not-allowed hover:bg-zinc-200 dark:hover:bg-zinc-700 transition active:scale-95"><i className="fas fa-arrow-left mr-2"/> Prev</button><div className="text-center px-4"><span className="block text-sm font-bold text-zinc-900 dark:text-[var(--text-color)]">Page {currentIndex+1}</span><span className="text-xs text-zinc-500">of {pages.length}</span></div><button onClick={nextPage} disabled={currentIndex===pages.length-1} aria-label="Next page" className="flex-1 py-3 rounded-xl bg-zinc-900 dark:bg-zinc-100 text-[var(--text-color)] dark:text-[var(--text-color)] font-medium disabled:opacity-30 disabled:cursor-not-allowed hover:bg-zinc-800 dark:hover:bg-zinc-200 transition active:scale-95">Next <i className="fas fa-arrow-right ml-2"/></button></div><p className="text-center text-[10px] text-zinc-400 mt-2">Tip: Swipe left/right or use arrow keys</p></div><div className="h-24"/></div>;
}