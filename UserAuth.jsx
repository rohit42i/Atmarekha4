import { useEffect, useState } from 'react';
import { createPortal } from 'react-dom';
import { supabase, getCurrentMembership } from './supabase';
import ProfileV2 from './ProfileV2';
import SubscriberBadge from './SubscriberBadge.jsx';

function routeNow() { return window.location.hash.replace(/^#/, '') || 'home'; }
function AuthPanel({ mode, setMode, onClose, onSignedIn }) { const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[confirmPassword,setConfirmPassword]=useState(''),[message,setMessage]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false); const submit=async event=>{event.preventDefault();if(busy)return;setBusy(true);setError('');setMessage('');try{if(mode==='signup'){if(!name.trim())throw new Error('Please enter your name.');if(password.length<6)throw new Error('Password must be at least 6 characters.');if(password!==confirmPassword)throw new Error('Passwords do not match.');const{data, error:authError}=await supabase.auth.signUp({email:email.trim(),password,options:{data:{full_name:name.trim()},emailRedirectTo:`${window.location.origin}/`}});if(authError)throw authError;if(data.session)onSignedIn();else setMessage('Check your email to verify your account, then sign in.')}else if(mode==='login'){const{error:authError}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(authError)throw authError;onSignedIn()}else{const{error:authError}=await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:`${window.location.origin}${window.location.pathname}#reset-password`});if(authError)throw authError;setMessage('Password reset instructions have been sent to your email.')}}catch(authError){setError(authError?.message||'Something went wrong. Please try again.')}finally{setBusy(false)}};const title=mode==='signup'?'Create your account':mode==='forgot'?'Reset your password':'Welcome back';return <div className="auth-panel-backdrop" role="presentation" onMouseDown={event=>{if(event.target===event.currentTarget)onClose()}}><section className="auth-panel" role="dialog" aria-modal="true" aria-labelledby="auth-panel-title"><button type="button" className="auth-panel-close" onClick={onClose} aria-label="Close">×</button><p className="section-eyebrow">ATMA REKHA</p><h2 id="auth-panel-title">{title}</h2><p className="auth-panel-subtitle">{mode==='signup'?'Create your reader account.':mode==='forgot'?'Enter your email and we’ll send you a secure reset link.':'Sign in to continue reading and access your profile.'}</p><form onSubmit={submit} className="auth-panel-form">{mode==='signup'&&<label>Name<input value={name} onChange={event=>setName(event.target.value)} autoComplete="name" required/></label>}<label>Email<input type="email" value={email} onChange={event=>setEmail(event.target.value)} autoComplete="email" required/></label>{mode!=='forgot'&&<label>Password<input type="password" value={password} onChange={event=>setPassword(event.target.value)} autoComplete={mode==='signup'?'new-password':'current-password'} required/></label>}{mode==='signup'&&<label>Confirm password<input type="password" value={confirmPassword} onChange={event=>setConfirmPassword(event.target.value)} autoComplete="new-password" required/></label>}<button type="submit" className="primary-button" disabled={busy}>{busy?'Please wait…':mode==='signup'?'Create account':mode==='forgot'?'Send reset link':'Sign in'}</button></form>{error&&<p className="auth-panel-error" role="alert">{error}</p>}{message&&<p className="auth-panel-message" role="status">{message}</p>}{mode==='login'&&<button className="auth-panel-link" onClick={()=>{setMode('forgot');setError('');setMessage('')}}>Forgot password?</button>}{mode==='forgot'&&<button className="auth-panel-link" onClick={()=>{setMode('login');setError('');setMessage('')}}>Back to sign in</button>}{mode!=='forgot'&&<p className="auth-panel-switch">{mode==='login'?'New to Atma Rekha?':'Already have an account?'} <button onClick={()=>{setMode(mode==='login'?'signup':'login');setError('');setMessage('')}}>{mode==='login'?'Create account':'Sign in'}</button></p>}</section></div> }
function ResetPasswordPanel({onClose}){const[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[message,setMessage]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async event=>{event.preventDefault();if(password.length<6){setError('Password must be at least 6 characters.');return}if(password!==confirm){setError('Passwords do not match.');return}setBusy(true);setError('');const{error:updateError}=await supabase.auth.updateUser({password});setBusy(false);if(updateError)setError(updateError.message);else setMessage('Password updated successfully.')};return <div className="auth-panel-backdrop"><section className="auth-panel" role="dialog" aria-modal="true"><button type="button" className="auth-panel-close" onClick={onClose} aria-label="Close">×</button><p className="section-eyebrow">ATMA REKHA</p><h2>Choose a new password</h2><form onSubmit={submit} className="auth-panel-form"><label>New password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><label>Confirm password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label><button className="primary-button" disabled={busy}>{busy?'Updating…':'Update password'}</button></form>{error&&<p className="auth-panel-error">{error}</p>}{message&&<p className="auth-panel-message">{message}</p>}</section></div>}
function UserMenu({user,profile,membershipPlan,onSignOut}){const[open,setOpen]=useState(false);const avatar=profile?.avatar_url||'';const name=profile?.display_name||user?.user_metadata?.full_name||'Reader';return <div className="user-auth-menu"><button type="button" className="user-auth-avatar-button" onClick={()=>setOpen(value=>!value)} aria-expanded={open} aria-label="Open account menu">{avatar?<img src={avatar} alt="" className="user-auth-avatar"/>:<span className="user-auth-avatar-fallback">{name.slice(0,1).toUpperCase()}</span>}<span className="user-auth-name">{name}<SubscriberBadge planId={membershipPlan}/></span><span className="user-auth-chevron">⌄</span></button>{open&&<div className="user-auth-dropdown"><div className="user-auth-dropdown-head"><strong>{name}<SubscriberBadge planId={membershipPlan}/></strong><span>{user.email}</span></div><button onClick={()=>{window.location.hash='profile';setOpen(false)}}>Profile</button><button onClick={()=>{window.location.hash='profile';window.localStorage.setItem('atma-profile-tab','bookmarks');setOpen(false)}}>Favourites</button><button onClick={()=>{window.location.hash='profile';window.localStorage.setItem('atma-profile-tab','history');setOpen(false)}}>History</button><button className="user-auth-signout" onClick={async()=>{setOpen(false);await onSignOut()}}>Sign out</button></div>}</div>}
export default function UserAuth(){const[route,setRoute]=useState(routeNow),[session,setSession]=useState(null),[profile,setProfile]=useState(null),[authMode,setAuthMode]=useState(null),[resetMode,setResetMode]=useState(false),[membershipPlan,setMembershipPlan]=useState(null);useEffect(()=>{const onHash=()=>setRoute(routeNow());window.addEventListener('hashchange',onHash);return()=>window.removeEventListener('hashchange',onHash)},[]);const loadProfile=async currentUser=>{if(!currentUser){setProfile(null);setMembershipPlan(null);return}const{data}=await supabase.from('profiles').select('*').eq('id',currentUser.id).maybeSingle();setProfile(data||null);try{setMembershipPlan(await getCurrentMembership(currentUser.id))}catch(error){console.warn('Membership lookup failed:',error);setMembershipPlan(null)}};useEffect(()=>{let alive=true;supabase.auth.getSession().then(({data})=>{if(!alive)return;setSession(data?.session||null);if(data?.session?.user)loadProfile(data.session.user)});const{data:listener}=supabase.auth.onAuthStateChange((_event,nextSession)=>{if(!alive)return;setSession(nextSession);if(nextSession?.user)loadProfile(nextSession.user);else{setProfile(null);setMembershipPlan(null)}});return()=>{alive=false;listener.subscription.unsubscribe()}},[]);const user=session?.user||null;const signOut=async()=>{await supabase.auth.signOut();window.location.hash='home'};const authControl=route==='home'?(user?<UserMenu user={user} profile={profile} membershipPlan={membershipPlan} onSignOut={signOut}/>:<button type="button" className="email-signin-button" onClick={()=>setAuthMode('login')}>Sign in</button>):null;const profilePortal=user&&route==='profile'?createPortal(<ProfileV2/>,document.body):null;const authPortal=authMode?createPortal(<AuthPanel mode={authMode} setMode={setAuthMode} onClose={()=>setAuthMode(null)} onSignedIn={()=>setAuthMode(null)}/>,document.body):null;const resetPortal=resetMode?createPortal(<ResetPasswordPanel onClose={()=>{setResetMode(false);window.location.hash='home'}}/>,document.body):null;useEffect(()=>{if(route==='reset-password')setResetMode(true)},[route]);return <>{authControl&&createPortal(<div className="user-auth-fixed">{authControl}</div>,document.body)}{profilePortal}{authPortal}{resetPortal}</>}
