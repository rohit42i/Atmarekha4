import { useEffect, useRef } from 'react';
import { supabase } from './supabase';
function getChapterId(){const route=window.location.hash.replace(/^#/,'');return route.startsWith('read-chapter/')?decodeURIComponent(route.slice('read-chapter/'.length)):null}
export default function ReadingHistoryTracker(){const userRef=useRef(null),lastSavedRef=useRef(''),timerRef=useRef(null);useEffect(()=>{let active=true;const save=async()=>{const user=userRef.current,chapterId=getChapterId();if(!active||!user||!chapterId)return;const pill=document.querySelector('.reader-page-pill'),text=pill?.textContent||'',match=text.match(/(\d+)\s*\/\s*(\d+)/),pageNumber=match?Number(match[1]):1;if(!Number.isFinite(pageNumber)||pageNumber<1)return;const key=`${user.id}:${chapterId}:${pageNumber}`;if(key===lastSavedRef.current)return;lastSavedRef.current=key;const{data:existing,error:findError}=await supabase.from('reading_history').select('id').eq('user_id',user.id).eq('chapter_id',chapterId).maybeSingle();if(findError){console.warn('Reading history lookup failed:',findError);return}if(existing?.id){const{error}=await supabase.from('reading_history').update({page_number:pageNumber,updated_at:new Date().toISOString()}).eq('id',existing.id);if(error)console.warn('Reading history update failed:',error)}else{const{error}=await supabase.from('reading_history').insert({user_id:user.id,chapter_id:chapterId,page_number:pageNumber});if(error)console.warn('Reading history insert failed:',error)}};const schedule=()=>{clearTimeout(timerRef.current);timerRef.current=setTimeout(save,500)};const onHash=()=>{lastSavedRef.current='';schedule()};const onReaderChange=()=>{if(document.querySelector('.reader-page-pill'))schedule()};window.addEventListener('hashchange',onHash);window.addEventListener('ar:reader-page',onReaderChange);supabase.auth.getSession().then(({data})=>{if(active){userRef.current=data?.session?.user||null;schedule()}});const{data:listener}=supabase.auth.onAuthStateChange((_event,session)=>{userRef.current=session?.user||null;lastSavedRef.current='';schedule()});const observer=new MutationObserver(m=>{if(m.some(x=>x.addedNodes.length||x.characterData))schedule()});observer.observe(document.body,{childList:true,subtree:true,characterData:true});schedule();return()=>{active=false;window.removeEventListener('hashchange',onHash);window.removeEventListener('ar:reader-page',onReaderChange);clearTimeout(timerRef.current);observer.disconnect();listener.subscription.unsubscribe()}},[]);return null}
