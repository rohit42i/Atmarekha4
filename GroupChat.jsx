import { useEffect, useMemo, useRef, useState } from 'react';
import { supabase } from './supabase';

const REACTIONS=['❤️','😂','😍','🔥','😭','😮','😎','🤯','🥹','😈','👀','👏','🙌','💀','🤡','🫶','✨','💯','🎉','🙏','😡','🤔','🥰','😴','🦚'];
const COOLDOWN=5000;
const ago=value=>{const d=Math.max(0,Date.now()-new Date(value).getTime()),m=Math.floor(d/60000);if(m<1)return'now';if(m<60)return`${m}m`;const h=Math.floor(m/60);if(h<24)return`${h}h`;return`${Math.floor(h/24)}d`};

export default function GroupChat(){
 const [route,setRoute]=useState(location.hash),[user,setUser]=useState(null),[messages,setMessages]=useState([]),[profiles,setProfiles]=useState({}),[reactions,setReactions]=useState({}),[text,setText]=useState(''),[loading,setLoading]=useState(true),[sending,setSending]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState(''),[picker,setPicker]=useState(null),[cooldown,setCooldown]=useState(0); const bottom=useRef(null),press=useRef(null);
 useEffect(()=>{const h=()=>setRoute(location.hash);addEventListener('hashchange',h);supabase.auth.getSession().then(({data})=>setUser(data?.session?.user||null));const{data:l}=supabase.auth.onAuthStateChange((_e,s)=>setUser(s?.user||null));return()=>{removeEventListener('hashchange',h);l.subscription.unsubscribe();clearTimeout(press.current)}},[]);
 const load=async()=>{setLoading(true);setError('');const{data:m,error:e}=await supabase.from('group_chat_messages').select('id,user_id,content,created_at').order('created_at',{ascending:true}).limit(100);if(e){setError(e.message);setLoading(false);return}setMessages(m||[]);const ids=[...new Set((m||[]).map(x=>x.user_id))];if(ids.length){const{data:p}=await supabase.from('profiles').select('id,username,display_name,avatar_url').in('id',ids);const map={};for(const x of p||[])map[x.id]=x;setProfiles(map)}const mids=(m||[]).map(x=>x.id);if(mids.length){const{data:r}=await supabase.from('group_chat_reactions').select('message_id,user_id,reaction').in('message_id',mids);const map={};for(const x of r||[]){map[x.message_id]??=[];map[x.message_id].push(x)}setReactions(map)}setLoading(false)};
 useEffect(()=>{if(route!=='#group-chat')return;load();const channel=supabase.channel('atma-rekha-group-chat').on('postgres_changes',{event:'INSERT',schema:'public',table:'group_chat_messages'},payload=>{setMessages(p=>p.some(x=>x.id===payload.new.id)?p:[...p,payload.new].slice(-100));}).on('postgres_changes',{event:'INSERT',schema:'public',table:'group_chat_reactions'},payload=>{setReactions(p=>({...p,[payload.new.message_id]:[...(p[payload.new.message_id]||[]),payload.new]}))}).on('postgres_changes',{event:'DELETE',schema:'public',table:'group_chat_reactions'},payload=>{setReactions(p=>({...p,[payload.old.message_id]:(p[payload.old.message_id]||[]).filter(x=>x.id!==payload.old.id)}))}).subscribe();return()=>{supabase.removeChannel(channel)}},[route]);
 useEffect(()=>{bottom.current?.scrollIntoView({behavior:'smooth'});},[messages.length]);
 useEffect(()=>{if(!cooldown)return;const t=setInterval(()=>setCooldown(v=>Math.max(0,v-100)),100);return()=>clearInterval(t)},[cooldown]);
 const send=async()=>{if(!user){setNotice('Sign in to join the group chat.');return}const content=text.trim();if(!content||sending)return;if(cooldown>0){setNotice(`Slow down — ${Math.ceil(cooldown/1000)}s`);return}setSending(true);setError('');const{error:e}=await supabase.from('group_chat_messages').insert({user_id:user.id,content});if(e)setError(e.message);else{setText('');setCooldown(COOLDOWN)}setSending(false)};
 const toggleReaction=async(messageId,reaction)=>{if(!user){setNotice('Sign in to react.');return}const own=(reactions[messageId]||[]).find(x=>x.user_id===user.id&&x.reaction===reaction);if(own){await supabase.from('group_chat_reactions').delete().eq('id',own.id)}else{await supabase.from('group_chat_reactions').insert({message_id:messageId,user_id:user.id,reaction})}setPicker(null)};
 const startHold=(id)=>{clearTimeout(press.current);press.current=setTimeout(()=>setPicker(id),3000)}; const endHold=()=>clearTimeout(press.current);
 const grouped=useMemo(()=>messages.map((m,i)=>({...m,profile:profiles[m.user_id],same:m.user_id===messages[i-1]?.user_id&&new Date(m.created_at)-new Date(messages[i-1]?.created_at)<300000})),[messages,profiles]);
 if(route!=='#group-chat')return null;
 return <main className="group-chat-page"><section className="group-chat-shell"><header className="group-chat-header"><button type="button" onClick={()=>location.hash='profile'} aria-label="Back">←</button><div className="group-chat-title-mark">✦</div><div><p>ATMA REKHA</p><h1>Community Group</h1><span>Everyone's here · text only</span></div><div className="group-chat-status"><i/>LIVE</div></header><div className="group-chat-rules"><span>💬 Be respectful</span><span>⏱ 5s between messages</span><span>🧹 7-day chat</span></div>{notice&&<div className="group-chat-notice">{notice}</div>}{error&&<div className="group-chat-error">{error}</div>}<div className="group-chat-feed">{loading?<div className="group-chat-loading"><div>✦</div><strong>Entering the community…</strong></div>:!grouped.length?<div className="group-chat-empty"><div>🌙</div><h2>The room is quiet.</h2><p>Be the first reader to say hello.</p></div>:grouped.map(m=>{const p=m.profile;const rs=reactions[m.id]||[];const counts={};rs.forEach(r=>counts[r.reaction]=(counts[r.reaction]||0)+1);return <article key={m.id} className={`group-message ${m.same?'same-user':''}`} onPointerDown={()=>startHold(m.id)} onPointerUp={endHold} onPointerCancel={endHold} onPointerLeave={endHold}><div className="group-avatar">{p?.avatar_url?<img src={p.avatar_url} alt=""/>:<span>{(p?.display_name||p?.username||'R').slice(0,1).toUpperCase()}</span>}</div><div className="group-message-main">{!m.same&&<div className="group-message-meta"><strong>{p?.display_name||p?.username||'Reader'}</strong><span>@{p?.username||'reader'} · {ago(m.created_at)}</span></div>}<div className="group-message-bubble">{m.content}</div>{Object.keys(counts).length>0&&<div className="group-reactions">{Object.entries(counts).map(([r,c])=><button key={r} type="button" onClick={()=>toggleReaction(m.id,r)} className={rs.some(x=>x.user_id===user?.id&&x.reaction===r)?'mine':''}>{r} <b>{c}</b></button>)}</div>}</div>{picker===m.id&&<div className="group-reaction-picker" onPointerDown={e=>e.stopPropagation()}><div className="group-reaction-picker-head"><span>React</span><button type="button" onClick={()=>setPicker(null)}>×</button></div><div>{REACTIONS.map(r=><button type="button" key={r} onClick={()=>toggleReaction(m.id,r)}>{r}</button>)}</div></div>}</article>})}<div ref={bottom}/></div><form className="group-composer" onSubmit={e=>{e.preventDefault();send()}}><div className="group-composer-inner"><textarea value={text} onChange={e=>setText(e.target.value.slice(0,700))} placeholder={user?'Say something to the community…':'Sign in to chat with everyone…'} maxLength={700} rows={1} disabled={!user||sending}/><span>{text.length}/700</span><button type="submit" disabled={!user||sending||!text.trim()||cooldown>0}>{cooldown>0?`${Math.ceil(cooldown/1000)}s`:'↑'}</button></div><small>Hold a message for 3 seconds to react · 25 reactions</small></form></section></main>;
}

export function GroupChatLauncher({user}){const[ready,setReady]=useState(false);useEffect(()=>{if(!user)return;const mount=()=>{const card=document.querySelector('.profile-v2-card');const community=card?.querySelector('.profile-community-card');if(!card||!community)return false;if(card.querySelector('.profile-group-chat-card')){setReady(true);return true}const button=document.createElement('button');button.type='button';button.className='profile-community-card profile-group-chat-card';button.innerHTML='<span class="profile-community-icon group-chat-launch-icon">💬</span><span><strong>Group Chat</strong><small>Talk with the Atma Rekha community</small></span><b>→</b>';button.addEventListener('click',()=>{location.hash='group-chat'});community.insertAdjacentElement('afterend',button);setReady(true);return true};mount();const o=new MutationObserver(mount);o.observe(document.body,{childList:true,subtree:true});return()=>o.disconnect()},[user]);return null}
